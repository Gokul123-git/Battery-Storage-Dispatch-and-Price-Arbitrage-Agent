# Optional. The whole numerical system (forecast, risk, LP, backtest, benchmark,
# agent, dashboard) runs WITHOUT any key. The key only enables an LLM rewrite of
# the operator note; without it a deterministic template is used.
ANTHROPIC_API_KEY=

# Optional overrides
GRIDFLEX_DATA_PATH=data/market_prices.parquet
